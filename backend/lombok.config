# hier ist die Konfiguration, such nicht in übergeordneten Ordnern
config.stopBubbling=true
# jeden Code, den lombok generiert soll eine Annotation haben, dass das von lombok generiert wurde
lombok.addLombokGeneratedAnnotation=true