export type Game = {
    id:string,
    name:string,
    releaseDate:string,
    note:string,
    fav:boolean
}

export type GameDto = {
    name:string,
    releaseDate:string,
    note:string,
    fav:boolean
}